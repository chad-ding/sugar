{
	"scripts": {
		"build": "lerna run build",
		"mock": "lerna run --scope @sugar/cli mock",
		"version": "lerna version --no-private",
		"release": "lerna publish --no-private",
		"beta": "lerna publish --canary --preid=beta"
	},
	"dependencies": {
		"lerna": "^8.2.3"
	},
	"devDependencies": {
		"rollup": "^4.45.1",
		"prettier": "^3.6.2"
	}
}
